<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:SleepingGodsDistantSkies.ViewModels"
             x:Class="SleepingGodsDistantSkies.Views.MapAreaPage"
             x:DataType="viewModels:MapAreaViewModel"
             Title="{Binding MapArea.Name}">
    <VerticalStackLayout>
        <CollectionView ItemsSource="{Binding MapArea.Locations}" SelectedItem="{Binding SelectedLocation}" >
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="viewModels:MapAreaViewModel">
                    <SwipeView>
                        <Grid Padding="5">
                            <Frame>
                                <Frame.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:MapAreaViewModel}}, Path=SelectLocationCommand}" CommandParameter="{Binding .}"/>
                                </Frame.GestureRecognizers>
                                <Label Text="{Binding .}" FontSize="24" />
                            </Frame>
                        </Grid>
                    </SwipeView>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <Button Text="Back" Command="{Binding GoBackCommand}" />
    </VerticalStackLayout>
</ContentPage>